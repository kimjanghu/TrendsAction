export const userState = {
  methods: {
    // getUserInfo() {
    //   const config = {
    //     headers: {
    //       token: `Bearer ${this.$cookies.get('auth-token')}`
    //     }
    //   }
    //   const userId = window.localStorage.getItem('userId')
    //   this.$http.get(this.$api.URL + this.$api.ROUTES.accounts.user + `?userId=${userId}`, config)
    //     .then(res => {
    //       return res.data.data
    //     })
    //     .catch(err => {
    //       console.log(err)
    //     })
    // }
  }
}